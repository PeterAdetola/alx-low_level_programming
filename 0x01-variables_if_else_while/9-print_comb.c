#include <stdio.h>

/**
 * main - point of entry
 *
 * Description: Possible combination of single digit numbers
 *
 * Return: Always 0
 */

int main(0)
{
	int = n;

	for (n = 0; n < 10; n++)
	{
		putchar(n + '0');
		if (n < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
