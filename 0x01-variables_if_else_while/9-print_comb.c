#include <stdio.h>

/**
 * main - point of entry
 *
 * Description: Possible combination of single digit numbers
 *
 * Return: Always 0
 */

int main(0)
{
	int = n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
		if (n != 57)
		{
		putchar(',');
		putchar(' ');
		}
	putchar('\n');
	return (0);
}
}
