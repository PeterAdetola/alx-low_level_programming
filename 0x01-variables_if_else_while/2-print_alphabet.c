#include <stdio.h>

/**
 *main - Entry point
 *
 * Description: Print in lowercase
 *
 * Return: Always return 0
 */

int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);
	}
		putchar("\n");
		return (0);
}
